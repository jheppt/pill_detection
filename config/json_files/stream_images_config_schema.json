{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Stream Images",
  "type": "object",
  "properties": {
    "dataset_type": {
      "type": "string",
      "enum": ["ogyeiv2", "synthetic", "nih", "cure", "hunyuan2"],
      "description": "The type of dataset being used."
    },
    "operation": {
      "type": "string",
      "enum": ["reference", "customer"],
      "description": "Type of operation being performed."
    },
    "threshold_area": {
      "type": "integer",
      "description": "Threshold area for the image processing."
    },
    "kernel_median_contour": {
      "type": "integer",
      "description": "Kernel size for the median filter applied to contours."
    },
    "canny_low_thr": {
      "type": "integer",
      "description": "Low threshold for Canny edge detection."
    },
    "canny_high_thr": {
      "type": "integer",
      "description": "High threshold for Canny edge detection."
    },
    "kernel_gaussian_texture": {
      "type": "integer",
      "description": "Kernel size for the Gaussian filter applied to textures."
    },
    "rotate_image": {
      "type": "boolean",
      "description": "Whether to rotate the image or not."
    },
    "image_size": {
      "type": "integer",
      "description": "Size of the image. Typically set to the same value as the image size in the Train config."
    },
    "max_worker": {
      "type": "integer",
      "description": "Number of maximum workers (threads) to use. Typically set to half the CPU cores."
    }
  },
  "required": ["dataset_type", "operation", "threshold_area", "kernel_median_contour", "canny_low_thr", "canny_high_thr", "kernel_gaussian_texture", "max_worker"]
}